#pragma once // Makes sure this file is included only once

const int WIDTH = 10;
const int HEIGHT = 10;    // The maze is 10 cells tall
const int CELL_SIZE = 80; // Each cell is 80 pixels 
const int MAX_COINS = 5;  // 5 coins
#define MIN(a,b) ((a) < (b) ? (a) : (b))